from gpkit import Variable, Model
class Payload(object):
	def __init__(x_dim,y_dim,z_dim):
	        self.x_dim = x_dim
	        self.y_dim = y_dim
	        self.z_dim = z_dim
	        self.W = W

#Payload array is an array of payload objects obviously
#Variables is an array of input variables, set to the model
def solve(payloadArr,variables,objective):
	print('yes')
	setVariables()
	return solutionObject

def setVariables():
	'w' : Variable('W',10,'kg','the wing weight')
solve([],[],[])